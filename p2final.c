#include<stdio.h>

struct_fraction
{
  int num,den;
};

typedef struct_fraction Fraction;
Fraction input_fraction()
{
  Fraction f;
  printf("Enter the fraction\n");
  scanf("%d%d",&f.num,&f.den);
  return f;
}

Fraction find_smallest(Fraction f2,Fraction f2,Fraction f3)
{
  int commmon_denominator =f1
}


